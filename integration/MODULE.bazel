module(name = "bazel_bats_integration", version = "0.0.0")

bazel_dep(name = "bazel_bats", version = "0.35.0")
bazel_dep(name = "rules_shell", version = "0.6.0")

local_path_override(
    module_name = "bazel_bats",
    path = "../"
)
